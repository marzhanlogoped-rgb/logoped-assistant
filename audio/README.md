Put voice files here:

- `greeting.mp3` - plays from the header greeting button.
- `reward-01.mp3` ... `reward-06.mp3` - play when the reward button is pressed.

The page still works if a file is missing; the browser just skips that sound.
