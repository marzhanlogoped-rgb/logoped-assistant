# Қалай оңай өзгертуге болады

## Карточка қосу

1. Файлды `cards` папкасына салыңыз.
2. Атын бар серия бойынша қойыңыз:
   - `plot11.jpeg` - тілдік бөлім, сюжетті суреттер.
   - `story11.jpeg` - тілдік бөлім, сюжеттік желілер.
   - `yes_no6.jpeg` - коммуникативтік бөлім, иә/жоқ.
   - `actions17.jpeg` - коммуникативтік бөлім, кім не істеді.
   - `because4.jpeg` - коммуникативтік бөлім, себеп-салдар.
   - `sentence12.jpeg` - коммуникативтік бөлім, сөйлем құрау.
   - `option11.jpeg` - коммуникативтік бөлім, дұрыс жауапты таңдау.
   - `request6.jpeg` - коммуникативтік бөлім, өтініш білдіру.
3. `index.html` ішіндегі `CARD_TYPES` бөлігінен сол серияның `count` санын көбейтіңіз.

## Атауларды өзгерту

`index.html` ішіндегі `CARD_TYPES` бөлімінде `title` мәтіндерін өзгертіңіз.

## Аудио

Қазіргі файлдар `.ogg` форматында қосылған. Chrome/Edge оларды ойнатады, MP3-ке ауыстыру міндетті емес.

## OpenAI кілті

Ең дұрыс жол: кілт браузерде емес, локал серверде тұрады.

1. `.env.example` файлын `.env` деп көшіріңіз.
2. `.env` ішіне өз кілтіңізді жазыңыз:
   `OPENAI_API_KEY=sk-...`
3. Сайтты `python server.py` арқылы ашыңыз.

GitHub-қа `.env` файлын салмаңыз. GitHub Pages тек статикалық сайт береді, құпия OpenAI кілтін қауіпсіз сақтай алмайды.
